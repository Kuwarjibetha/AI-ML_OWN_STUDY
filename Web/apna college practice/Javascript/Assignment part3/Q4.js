// Qs4. Write a JavaScript program to test whether the character at the given(character)index is lowercase.
let user = prompt("enter your string here....")
if(user == user.toLowerCase()){
   console.log("it is a lowercase");
}
else{
    console.log("this is not a lowercase");
}